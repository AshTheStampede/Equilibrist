# Equinix — Python dependencies
# Install with:  pip install -r requirements.txt

# Core app
streamlit>=1.32

# Numerics
numpy>=1.24
scipy>=1.11
pandas>=2.0

# Plotting
plotly>=5.18
matplotlib>=3.7          # required for publication-quality PNG export

# File I/O
openpyxl>=3.1
Pillow>=10.0

# Plotly static image export (kaleido)
# Windows requires the pinned 0.2.1 build; other platforms can use any 0.2+
kaleido==0.2.1; sys_platform == "win32"
kaleido>=0.2;  sys_platform != "win32"
